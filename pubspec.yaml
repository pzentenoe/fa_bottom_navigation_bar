name: fa_bottom_navigation_bar
description: Configurable bottom navigation bar with raised highlight and shadow based in ff_navigation_bar but allow fontawesome icons.
version: 0.0.1
author: pzentenoe
homepage:

environment:
  sdk: ">=2.3.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.0.2
  font_awesome_flutter: ^8.8.1

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
